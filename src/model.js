const euro = new Intl.NumberFormat('nl-NL', { style: 'currency', currency: 'EUR', minimumFractionDigits: 0, maximumFractionDigits: 2 });
export const formatEuro = (value) => euro.format(value || 0);
export const createInsurance = (id, naam) => ({ id, naam, maandpremie: 154, eigenRisico: 385, tandartsVergoeding: 0, tandartsPercentage: 0, fysioSessiesVergoed: 0, brilVergoeding: 0, brilPercentage: 0, alternatiefMaxVergoeding: 0, alternatiefPerSessie: 0, checks: { zorgverlener: false, toestemming: false, acceptatie: false, eigenBijdrage: false, lopendeBehandeling: false }, notitie: '' });
export const defaultState = { zorggebruik: { zorgOnderEigenRisico: 0, tandarts: 0, fysioSessies: 0, fysioKostenPerSessie: 39, bril: 0, alternatiefSessies: 0, alternatiefKostenPerSessie: 65, overigeEigenKosten: 0 }, verzekeringen: [] };
const numberOr = (value, fallback) => { const parsed = Number.parseFloat(value); return Number.isFinite(parsed) ? Math.max(0, parsed) : fallback; };
const normalizeInsurance = (verzekering, index) => { const fallback = createInsurance(index + 1, `Polis ${index + 1}`); if (!verzekering || typeof verzekering !== 'object') return fallback; return { ...fallback, ...Object.fromEntries(Object.keys(fallback).filter((key) => key !== 'checks').map((key) => { if (key === 'id') return [key, index + 1]; if (key === 'naam' || key === 'notitie') return [key, typeof verzekering[key] === 'string' ? verzekering[key] : fallback[key]]; return [key, numberOr(verzekering[key], fallback[key])]; })), checks: Object.fromEntries(Object.keys(fallback.checks).map((key) => [key, Boolean(verzekering.checks?.[key])])) }; };
export const normalizeState = (input) => { if (!input || typeof input !== 'object' || !Array.isArray(input.verzekeringen) || typeof input.zorggebruik !== 'object') throw new Error('Ongeldig bestandsformaat'); const legacy = input.zorggebruik; return { zorggebruik: { ...defaultState.zorggebruik, zorgOnderEigenRisico: numberOr(legacy.zorgOnderEigenRisico ?? legacy.overigOnderEigenRisico, 0), tandarts: numberOr(legacy.tandarts, 0), fysioSessies: numberOr(legacy.fysioSessies, 0), fysioKostenPerSessie: numberOr(legacy.fysioKostenPerSessie, defaultState.zorggebruik.fysioKostenPerSessie), bril: numberOr(legacy.bril, 0), alternatiefSessies: numberOr(legacy.alternatiefSessies, 0), alternatiefKostenPerSessie: numberOr(legacy.alternatiefKostenPerSessie, defaultState.zorggebruik.alternatiefKostenPerSessie), overigeEigenKosten: numberOr(legacy.overigeEigenKosten, 0) }, verzekeringen: input.verzekeringen.map(normalizeInsurance) }; };
const clampPercentage = (value) => Math.min(100, Math.max(0, numberOr(value, 0)));
const berekenCategorieVergoeding = (kosten, maximum, percentage) => { if (kosten <= 0) return 0; const hasMaximum = maximum > 0; const hasPercentage = clampPercentage(percentage) > 0; if (!hasMaximum && !hasPercentage) return 0; const opPercentage = hasPercentage ? kosten * (clampPercentage(percentage) / 100) : kosten; return Math.min(kosten, hasMaximum ? Math.min(opPercentage, maximum) : opPercentage); };
export const berekenKosten = (verzekering, zorggebruik) => { const jaarPremie = verzekering.maandpremie * 12; const fysioTotaal = zorggebruik.fysioSessies * zorggebruik.fysioKostenPerSessie; const alternatiefTotaal = zorggebruik.alternatiefSessies * zorggebruik.alternatiefKostenPerSessie; const alternatiefVergoeding = Math.min(alternatiefTotaal, zorggebruik.alternatiefSessies * Math.min(zorggebruik.alternatiefKostenPerSessie, verzekering.alternatiefPerSessie), verzekering.alternatiefMaxVergoeding); const breakdown = { tandarts: Math.max(0, zorggebruik.tandarts - berekenCategorieVergoeding(zorggebruik.tandarts, verzekering.tandartsVergoeding, verzekering.tandartsPercentage)), fysio: Math.max(0, fysioTotaal - Math.min(zorggebruik.fysioSessies, verzekering.fysioSessiesVergoed) * zorggebruik.fysioKostenPerSessie), bril: Math.max(0, zorggebruik.bril - berekenCategorieVergoeding(zorggebruik.bril, verzekering.brilVergoeding, verzekering.brilPercentage)), alternatief: Math.max(0, alternatiefTotaal - alternatiefVergoeding), overig: zorggebruik.overigeEigenKosten }; const eigenRisicoGebruikt = Math.min(zorggebruik.zorgOnderEigenRisico, verzekering.eigenRisico); const eigenKostenAanvullend = Object.values(breakdown).reduce((total, value) => total + value, 0); return { totaal: jaarPremie + eigenRisicoGebruikt + eigenKostenAanvullend, jaarPremie, eigenRisicoGebruikt, eigenKostenAanvullend, breakdown }; };
const driverLabels = { premie: 'Jaarpremie', eigenRisico: 'Eigen risico', tandarts: 'Tandarts en mondzorg', fysio: 'Fysiotherapie', bril: 'Brillen en lenzen', alternatief: 'Alternatieve zorg', overig: 'Andere eigen kosten' };
export const getKostenDrivers = (kosten) => [{ key: 'premie', value: kosten.jaarPremie }, { key: 'eigenRisico', value: kosten.eigenRisicoGebruikt }, ...Object.entries(kosten.breakdown).map(([key, value]) => ({ key, value }))].filter((item) => item.value > 0).sort((a, b) => b.value - a.value).slice(0, 3).map((item) => ({ ...item, label: driverLabels[item.key] }));
