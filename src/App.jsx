import React, { useEffect, useRef, useState } from 'react';
import { Calculator, CheckCircle2, ChevronLeft, ChevronRight, Copy, Download, FileText, Plus, Settings2, ShieldCheck, Trash2, Upload } from 'lucide-react';
import { berekenKosten, createInsurance, defaultState, formatEuro, getKostenDrivers, normalizeState } from './model.js';

const STORAGE_KEY = 'zorgvergelijker-state-v2';
const LEGACY_STORAGE_KEY = 'zorgvergelijker-state-v1';
const stappen = ['zorggebruik', 'polissen', 'vergelijking'];

const stapMeta = {
  zorggebruik: { label: 'Vul je zorggebruik in', stap: 'Stap 1', beschrijving: 'Vul alleen kosten in die je goed kunt inschatten.' },
  polissen: { label: 'Voeg je polissen toe', stap: 'Stap 2', beschrijving: 'Vergelijk premie, eigen risico en aanvullende vergoedingen.' },
  vergelijking: { label: 'Vergelijk de jaarlasten', stap: 'Stap 3', beschrijving: 'Bekijk welke handmatig ingevoerde optie het laagst uitkomt.' }
};

const zorgGroepen = [
  { id: 'eigen-risico', title: 'Zorg die onder je eigen risico valt', description: 'Schat het totaal van zorg waarvoor je komend jaar waarschijnlijk eigen risico betaalt.', velden: [{ key: 'zorgOnderEigenRisico', label: 'Verwachte zorgkosten onder eigen risico', hint: 'Bijvoorbeeld ziekenhuis, specialist of medicijnen. Huisartszorg valt meestal niet onder het eigen risico.' }] },
  { id: 'tandarts', title: 'Tandarts en mondzorg', description: 'Alle verwachte tandartskosten voor een jaar.', velden: [{ key: 'tandarts', label: 'Tandartskosten per jaar', hint: 'Bijvoorbeeld controles, mondhygiënist en vullingen.' }] },
  { id: 'fysio', title: 'Fysiotherapie en beweegzorg', description: 'Aantal behandelingen en de gemiddelde prijs vormen samen je inschatting.', velden: [{ key: 'fysioSessies', label: 'Aantal behandelingen', hint: 'Per jaar', step: '1' }, { key: 'fysioKostenPerSessie', label: 'Kosten per behandeling', hint: 'Gemiddelde prijs per behandeling' }] },
  { id: 'bril', title: 'Brillen en lenzen', description: 'Gebruik dit als totaalbedrag voor bril, glazen of lenzen.', velden: [{ key: 'bril', label: 'Kosten per jaar', hint: 'Alleen wat je dit jaar verwacht uit te geven.' }] },
  { id: 'alternatief', title: 'Alternatieve zorg', description: 'Aantal behandelingen en de prijs per behandeling vormen samen je jaarinschatting.', velden: [{ key: 'alternatiefSessies', label: 'Aantal behandelingen', hint: 'Per jaar', step: '1' }, { key: 'alternatiefKostenPerSessie', label: 'Kosten per behandeling', hint: 'Bijvoorbeeld acupunctuur of osteopathie.' }] },
  { id: 'overig', title: 'Andere voorspelbare eigen kosten', description: 'Voor kosten die je zelf verwacht te betalen en die niet in de velden hierboven passen.', velden: [{ key: 'overigeEigenKosten', label: 'Andere eigen kosten per jaar', hint: 'Wordt niet gekoppeld aan een polisvergoeding.' }] }
];

const polisGroepen = [
  { id: 'basis', label: 'Premie en eigen risico', beschrijving: 'Vul de totale premie in: basisverzekering plus eventuele aanvullende verzekering.', velden: [{ key: 'maandpremie', label: 'Totale maandpremie', hint: 'Basisverzekering plus aanvullende pakketten.', kind: 'currency' }, { key: 'eigenRisico', label: 'Gekozen eigen risico', hint: 'Verplicht en eventueel vrijwillig eigen risico samen.', kind: 'currency' }] },
  { id: 'tandarts', label: 'Tandarts en mondzorg', beschrijving: 'Vul het maximum, percentage of beide in als dit pakket tandartszorg vergoedt.', velden: [{ key: 'tandartsVergoeding', label: 'Maximum per jaar', kind: 'currency' }, { key: 'tandartsPercentage', label: 'Vergoeding in procenten', kind: 'percentage' }] },
  { id: 'fysio', label: 'Fysiotherapie en beweegzorg', beschrijving: 'Deze rekenhulp gaat uit van volledig vergoede sessies tot dit aantal.', velden: [{ key: 'fysioSessiesVergoed', label: 'Aantal vergoede behandelingen', kind: 'number' }] },
  { id: 'bril', label: 'Brillen en lenzen', beschrijving: 'Vul het maximum, percentage of beide in als dit pakket brillen of lenzen vergoedt.', velden: [{ key: 'brilVergoeding', label: 'Maximum per jaar', kind: 'currency' }, { key: 'brilPercentage', label: 'Vergoeding in procenten', kind: 'percentage' }] },
  { id: 'alternatief', label: 'Alternatieve zorg', beschrijving: 'Vul zowel het maximum per jaar als de vergoeding per behandeling in.', velden: [{ key: 'alternatiefMaxVergoeding', label: 'Maximum per jaar', kind: 'currency' }, { key: 'alternatiefPerSessie', label: 'Vergoeding per behandeling', kind: 'currency' }] }
];

const checklist = [
  'Is jouw vaste ziekenhuis, therapeut, kliniek of andere zorgverlener gecontracteerd?',
  'Heb je voor deze zorg een verwijzing, toestemming of machtiging nodig?',
  'Geldt er een medische acceptatieprocedure of wachttijd voor de aanvullende verzekering?',
  'Is er een wettelijke eigen bijdrage die je naast het eigen risico betaalt?',
  'Ben je al onder behandeling en is het duidelijk welke verzekeraar de rekening verwerkt?'
];

const toNumber = (value) => {
  const parsed = Number.parseFloat(value);
  return Number.isFinite(parsed) ? Math.max(0, parsed) : 0;
};

const safeLoadState = () => {
  if (typeof window === 'undefined') return defaultState;
  try {
    const raw = window.localStorage.getItem(STORAGE_KEY) || window.localStorage.getItem(LEGACY_STORAGE_KEY);
    return raw ? normalizeState(JSON.parse(raw)) : defaultState;
  } catch {
    return defaultState;
  }
};

const renderPrintHtml = ({ zorggebruik, resultaten, goedkoopste }) => {
  const items = [
    ['Zorg onder eigen risico', zorggebruik.zorgOnderEigenRisico],
    ['Tandarts en mondzorg', zorggebruik.tandarts],
    ['Fysiotherapie', zorggebruik.fysioSessies ? `${zorggebruik.fysioSessies} x ${formatEuro(zorggebruik.fysioKostenPerSessie)}` : 0],
    ['Brillen en lenzen', zorggebruik.bril],
    ['Alternatieve zorg', zorggebruik.alternatiefSessies ? `${zorggebruik.alternatiefSessies} x ${formatEuro(zorggebruik.alternatiefKostenPerSessie)}` : 0],
    ['Andere eigen kosten', zorggebruik.overigeEigenKosten]
  ].filter(([, value]) => value !== 0 && value !== '0 x € 0').map(([label, value]) => `<div class="usage-item"><span>${label}</span><strong>${typeof value === 'number' ? formatEuro(value) : value}</strong></div>`).join('');
  const rows = resultaten.map(({ verzekering, kosten }, index) => `<section class="plan ${goedkoopste?.verzekering.id === verzekering.id ? 'best' : ''}"><header><div><p class="kicker">${index === 0 ? 'Laagste berekende jaarlast' : `Optie ${index + 1}`}</p><h2>${verzekering.naam}</h2></div><div class="price">${formatEuro(kosten.totaal)}</div></header><div class="grid"><div><span>Jaarpremie</span><strong>${formatEuro(kosten.jaarPremie)}</strong></div><div><span>Eigen risico gebruikt</span><strong>${formatEuro(kosten.eigenRisicoGebruikt)}</strong></div><div><span>Andere eigen kosten</span><strong>${formatEuro(kosten.eigenKostenAanvullend)}</strong></div></div></section>`).join('');
  return `<!doctype html><html lang="nl"><head><meta charset="utf-8" /><title>Zorgvergelijker overzicht</title><style>body{font-family:Arial,sans-serif;margin:0;padding:40px;color:#102a43;background:#f8fafc}h1,h2{margin:0}.hero{padding:28px;border-radius:24px;background:linear-gradient(135deg,#0f766e,#164e63);color:#fff;margin-bottom:24px}.usage{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin:24px 0}.usage-item,.grid>div{background:#fff;border-radius:16px;padding:14px 16px;border:1px solid #d9e2ec;display:flex;justify-content:space-between;gap:16px}.plan{background:#fff;border-radius:24px;padding:24px;margin-bottom:16px;border:1px solid #d9e2ec}.plan.best{border-color:#0f766e}.kicker{text-transform:uppercase;letter-spacing:.12em;font-size:12px;margin:0 0 6px;color:#486581}header{display:flex;justify-content:space-between;gap:24px;align-items:flex-start;margin-bottom:18px}.price{font-size:34px;font-weight:700;color:#0f766e}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.grid span{display:block;color:#627d98;font-size:12px;margin-bottom:6px}.notice{margin:24px 0;padding:16px;border-radius:16px;background:#fff7ed;border:1px solid #fed7aa;line-height:1.5}@media print{body{padding:20px}}</style></head><body><div class="hero"><h1>Zorgvergelijker</h1><p>Jaarlasten op basis van jouw eigen inschatting.</p></div><div class="notice"><strong>Let op:</strong> deze rekenhulp controleert geen zorgverleners, voorwaarden, toestemming, wettelijke eigen bijdragen of wachttijden. Controleer deze altijd bij de verzekeraar.</div><div class="usage">${items || '<div class="usage-item"><span>Geen kosten ingevuld</span><strong>€ 0</strong></div>'}</div>${rows}</body></html>`;
};

export default function App() {
  const [storedState] = useState(safeLoadState);
  const [zorggebruik, setZorggebruik] = useState(storedState.zorggebruik);
  const [verzekeringen, setVerzekeringen] = useState(storedState.verzekeringen);
  const [actieveStap, setActieveStap] = useState('zorggebruik');
  const [openPolissen, setOpenPolissen] = useState(() => [storedState.verzekeringen[0]?.id].filter(Boolean));
  const [settingsOpen, setSettingsOpen] = useState(false);
  const fileInputRef = useRef(null);
  const stappenRef = useRef(null);
  const settingsRef = useRef(null);

  useEffect(() => { window.localStorage.setItem(STORAGE_KEY, JSON.stringify({ versie: 2, zorggebruik, verzekeringen })); }, [zorggebruik, verzekeringen]);
  const resultaten = verzekeringen.map((verzekering) => ({ verzekering, kosten: berekenKosten(verzekering, zorggebruik) })).sort((a, b) => a.kosten.totaal - b.kosten.totaal);
  const goedkoopste = resultaten[0] ?? null;
  const updateZorggebruik = (key, value) => setZorggebruik((current) => ({ ...current, [key]: toNumber(value) }));
  const updateVerzekering = (id, key, value) => setVerzekeringen((current) => current.map((verzekering) => verzekering.id === id ? { ...verzekering, [key]: key === 'naam' || key === 'notitie' ? value : toNumber(value) } : verzekering));
  const voegVerzekeringToe = () => { const nextId = Math.max(0, ...verzekeringen.map((verzekering) => verzekering.id)) + 1; setVerzekeringen((current) => [...current, createInsurance(nextId, `Nieuwe polis ${nextId}`)]); setOpenPolissen([nextId]); };
  const verwijderVerzekering = (id) => { if (verzekeringen.length === 1) return; const next = verzekeringen.filter((verzekering) => verzekering.id !== id); setVerzekeringen(next); setOpenPolissen([next[0]?.id].filter(Boolean)); };
  const dupliceerVerzekering = (id) => { const bron = verzekeringen.find((verzekering) => verzekering.id === id); if (!bron) return; const nextId = Math.max(0, ...verzekeringen.map((verzekering) => verzekering.id)) + 1; const kopie = { ...bron, id: nextId, naam: `${bron.naam} kopie` }; setVerzekeringen((current) => { const index = current.findIndex((verzekering) => verzekering.id === id); return [...current.slice(0, index + 1), kopie, ...current.slice(index + 1)]; }); setOpenPolissen([nextId]); };
  const exporteerJson = () => { const blob = new Blob([JSON.stringify({ versie: 2, datum: new Date().toISOString(), zorggebruik, verzekeringen }, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = `zorgvergelijker-${new Date().toISOString().slice(0, 10)}.json`; document.body.appendChild(link); link.click(); link.remove(); URL.revokeObjectURL(url); };
  const importeerJson = async (event) => { const file = event.target.files?.[0]; if (!file) return; try { const next = normalizeState(JSON.parse(await file.text())); setZorggebruik(next.zorggebruik); setVerzekeringen(next.verzekeringen); setOpenPolissen([next.verzekeringen[0]?.id].filter(Boolean)); window.alert('Bestand geïmporteerd.'); } catch { window.alert('Dit bestand kon niet worden geïmporteerd. Kies een geldig Zorgvergelijker-bestand.'); } finally { event.target.value = ''; } };
  const exporteerPrint = () => { const frame = document.createElement('iframe'); frame.setAttribute('aria-hidden', 'true'); frame.style.cssText = 'position:fixed;right:0;bottom:0;width:0;height:0;border:0'; document.body.appendChild(frame); const printWindow = frame.contentWindow; if (!printWindow) { frame.remove(); return; } frame.addEventListener('load', () => { printWindow.focus(); printWindow.print(); window.setTimeout(() => frame.remove(), 1200); }, { once: true }); printWindow.document.open(); printWindow.document.write(renderPrintHtml({ zorggebruik, resultaten, goedkoopste })); printWindow.document.close(); };
  const gaNaarStap = (stap) => { setActieveStap(stap); window.requestAnimationFrame(() => stappenRef.current?.scrollIntoView({ behavior: 'smooth', block: 'start' })); };
  const toggleInstellingen = () => {
    setSettingsOpen((current) => {
      const next = !current;
      if (next) {
        window.requestAnimationFrame(() => window.requestAnimationFrame(() => {
          const top = (settingsRef.current?.getBoundingClientRect().top ?? 0) + window.scrollY - 8;
          window.scrollTo({ top: Math.max(0, top), behavior: 'auto' });
        }));
      }
      return next;
    });
  };
  const actieveStapIndex = stappen.indexOf(actieveStap);

  return <main className="page-shell">
    <section className="hero-card"><div className="hero-copy"><div className="eyebrow"><ShieldCheck size={16} />Nederlands, handmatig en privacyvriendelijk</div><h1>Zorgvergelijker voor je verwachte jaarlasten</h1><p>Vergelijk je totale premie, eigen risico en een paar voorspelbare extra zorgkosten. Zo krijg je snel een helder kostenplaatje zonder te doen alsof iedere polisregel te berekenen is.</p><div className="hero-actions"><button type="button" className="primary-button" onClick={exporteerJson}><Download size={18} />Exporteer JSON</button><button type="button" className="secondary-button" onClick={toggleInstellingen} aria-expanded={settingsOpen} aria-controls="instellingen-paneel"><Settings2 size={18} />Instellingen</button><button type="button" className="secondary-button" onClick={() => fileInputRef.current?.click()}><Upload size={18} />Importeer</button><button type="button" className="secondary-button" onClick={exporteerPrint}><FileText size={18} />Print of PDF</button><input ref={fileInputRef} type="file" accept=".json,application/json" hidden onChange={importeerJson} /></div></div><aside className="hero-summary"><div className="summary-kicker"><Calculator size={16} />Huidige samenvatting</div><div className="summary-price">{formatEuro(goedkoopste?.kosten.totaal)}</div><p>{goedkoopste ? `${goedkoopste.verzekering.naam} heeft nu de laagste berekende jaarlast.` : 'Voeg minimaal één polis toe om te vergelijken.'}</p><ul className="summary-points"><li>Autosave staat standaard aan in je browser.</li><li>Je data blijft lokaal tot je zelf exporteert.</li><li>Controleer voorwaarden altijd bij de verzekeraar.</li></ul></aside></section>
    {settingsOpen ? <section className="settings-panel" id="instellingen-paneel" ref={settingsRef}><div className="settings-header"><div><p className="section-kicker">Instellingen</p><h2>Wat deze rekenhulp wel en niet berekent</h2></div><p>De invoer is bewust beperkt tot kosten die je zelf goed kunt inschatten. De punten hieronder zijn wel belangrijk, maar worden niet meegerekend.</p></div><div className="checklist-grid">{checklist.map((item) => <div key={item} className="checklist-item"><CheckCircle2 size={19} /><span>{item}</span></div>)}</div></section> : null}
    <section className="workspace-tabs" aria-label="Stappen" ref={stappenRef}>{stappen.map((stap) => <button key={stap} type="button" className={`workflow-card workflow-tab ${actieveStap === stap ? 'is-active' : ''}`} onClick={() => gaNaarStap(stap)}><span className="section-kicker">{stapMeta[stap].stap}</span><strong>{stapMeta[stap].label}</strong><p>{stapMeta[stap].beschrijving}</p></button>)}</section>
    <section className="tab-shell">
      {actieveStap === 'zorggebruik' ? <section className="tab-panel" id="step-zorggebruik"><div className="step-intro tab-intro"><p className="section-kicker">Stap 1</p><h2>Verwachte kosten</h2><p>Vul alleen kosten in die je redelijk kunt voorspellen. De zorg onder eigen risico wordt per polis automatisch afgetopt op het gekozen eigen risico.</p></div><article className="panel"><div className="zorggroepen-grid">{zorgGroepen.map((groep) => <section key={groep.id} className={`zorggroep-card ${groep.velden.length > 1 ? 'zorggroep-card-paired' : ''}`}><div className="zorggroep-header"><h3>{groep.title}</h3><p>{groep.description}</p></div><div className={`field-grid ${groep.velden.length > 1 ? 'field-grid-paired' : ''}`}>{groep.velden.map((veld) => <label key={veld.key} className="field"><span>{veld.label}</span><input type="number" min="0" step={veld.step || '0.01'} value={zorggebruik[veld.key]} onChange={(event) => updateZorggebruik(veld.key, event.target.value)} /><small>{veld.hint}</small></label>)}</div></section>)}</div></article></section> : null}
      {actieveStap === 'polissen' ? <section className="tab-panel" id="step-polissen"><div className="step-intro step-intro-with-action tab-intro"><div><p className="section-kicker">Stap 2</p><h2>Polissen invoeren</h2><p>Vul per polis de totale maandpremie in, inclusief eventuele aanvullende verzekering. Alleen de vier voorspelbare aanvullende vergoedingen worden meegenomen.</p></div><button type="button" className="primary-button step-button" onClick={voegVerzekeringToe}><Plus size={18} />Polis toevoegen</button></div><section className="insurance-stack">{verzekeringen.map((verzekering, index) => { const polisOpen = openPolissen.includes(verzekering.id); return <article key={verzekering.id} className="insurance-card"><div className="insurance-card-top"><div className="insurance-header"><label className="field grow"><span className="polis-label-row"><span>Naam van de polis</span><span className="polis-badge">Polis {index + 1}</span></span><input type="text" value={verzekering.naam} onChange={(event) => updateVerzekering(verzekering.id, 'naam', event.target.value)} /></label><div className="policy-actions"><button type="button" className="mini-button" onClick={() => dupliceerVerzekering(verzekering.id)}><Copy size={16} />Dupliceer</button><button type="button" className="mini-button" onClick={() => setOpenPolissen(polisOpen ? [] : [verzekering.id])}>{polisOpen ? 'Inklappen' : 'Uitklappen'}</button><button type="button" className="icon-button" onClick={() => verwijderVerzekering(verzekering.id)} disabled={verzekeringen.length === 1} aria-label={`Verwijder ${verzekering.naam}`}><Trash2 size={18} /></button></div></div>{!polisOpen ? <div className="policy-collapsed-summary"><span>{formatEuro(verzekering.maandpremie)} per maand</span><span>Eigen risico: {formatEuro(verzekering.eigenRisico)}</span>{verzekering.notitie ? <span>Notitie toegevoegd</span> : null}</div> : null}</div>{polisOpen ? <div className="policy-sections">{polisGroepen.map((groep) => <section key={groep.id} className="policy-section"><div className="policy-section-header"><h3>{groep.label}</h3><p>{groep.beschrijving}</p></div><div className="field-grid policy-grid">{groep.velden.map((veld) => <label key={veld.key} className="field"><span>{veld.label}</span><input type="number" min="0" max={veld.kind === 'percentage' ? '100' : undefined} step={veld.kind === 'number' || veld.kind === 'percentage' ? '1' : '0.01'} value={verzekering[veld.key]} onChange={(event) => updateVerzekering(verzekering.id, veld.key, event.target.value)} />{veld.hint ? <small>{veld.hint}</small> : null}</label>)}</div></section>)}<section className="policy-section policy-note-section"><div className="policy-section-header"><h3>Notitie bij deze polis</h3><p>Noteer bijvoorbeeld een wachttijd, voorwaarden of een zorgverlener die je nog moet controleren.</p></div><label className="field"><span>Notitie</span><textarea rows="3" value={verzekering.notitie} onChange={(event) => updateVerzekering(verzekering.id, 'notitie', event.target.value)} placeholder="Bijvoorbeeld: alleen vergoed bij gecontracteerde behandelaar." /></label></section></div> : null}</article>; })}</section><div className="policy-add-footer"><button type="button" className="secondary-button" onClick={voegVerzekeringToe}><Plus size={18} />Polis toevoegen</button></div></section> : null}
      {actieveStap === 'vergelijking' ? <section className="tab-panel" id="step-vergelijking"><div className="step-intro tab-intro"><p className="section-kicker">Stap 3</p><h2>Vergelijking</h2><p>Dit is een kostenvergelijking op basis van jouw invoer, geen verzekeringsadvies.</p></div><div className="results-layout"><article className="panel insight-panel"><div className="insight-stack"><div className="insight-card highlight"><span>Laagste berekende jaarlast</span><strong>{goedkoopste?.verzekering.naam || 'Nog niet beschikbaar'}</strong><p>{goedkoopste ? formatEuro(goedkoopste.kosten.totaal) : 'Vul een polis in om te vergelijken.'}</p></div><div className="insight-card"><span>Belangrijk</span><strong>Controleer altijd de voorwaarden</strong><p>Deze tool controleert geen zorgverleners, toestemming, wettelijke eigen bijdragen, acceptatie of wachttijden.</p></div></div></article><section className="results-grid">{resultaten.map((resultaat, index) => <article key={resultaat.verzekering.id} className={`result-card ${index === 0 ? 'is-best' : ''}`} data-testid="result-card"><div className="result-topline"><div><p className="result-rank">{index === 0 ? 'Laagste berekende jaarlast' : `Plaats ${index + 1}`}</p><h3>{resultaat.verzekering.naam}</h3></div><div className="result-total">{formatEuro(resultaat.kosten.totaal)}</div></div><div className="result-highlights"><span className="result-highlights-label">Belangrijkste kostenposten</span><div className="result-highlights-list">{getKostenDrivers(resultaat.kosten).map((driver) => <div key={driver.key} className="highlight-pill"><span>{driver.label}</span><strong>{formatEuro(driver.value)}</strong></div>)}</div></div><div className="metric-row"><div className="metric-box"><span>Jaarpremie</span><strong>{formatEuro(resultaat.kosten.jaarPremie)}</strong></div><div className="metric-box"><span>Eigen risico gebruikt</span><strong>{formatEuro(resultaat.kosten.eigenRisicoGebruikt)}</strong></div><div className="metric-box"><span>Andere eigen kosten</span><strong>{formatEuro(resultaat.kosten.eigenKostenAanvullend)}</strong></div></div><div className="difference-bar">{index === 0 ? 'Referentiepunt voor deze vergelijking' : `${formatEuro(resultaat.kosten.totaal - goedkoopste.kosten.totaal)} duurder dan de laagste berekende optie`}</div><details className="breakdown"><summary>Bekijk opbouw van de eigen kosten</summary><div className="breakdown-grid">{Object.entries(resultaat.kosten.breakdown).map(([key, value]) => <div key={key}>{({ tandarts: 'Tandarts en mondzorg', fysio: 'Fysiotherapie', bril: 'Brillen en lenzen', alternatief: 'Alternatieve zorg', overig: 'Andere eigen kosten' })[key]}: {formatEuro(value)}</div>)}</div></details>{resultaat.verzekering.notitie ? <div className="policy-note-box"><span className="result-highlights-label">Opmerking bij deze polis</span><p>{resultaat.verzekering.notitie}</p></div> : null}</article>)}</section></div></section> : null}
      <div className="tab-footer">{actieveStapIndex > 0 ? <button type="button" className="secondary-button" onClick={() => gaNaarStap(stappen[actieveStapIndex - 1])}><ChevronLeft size={18} />Vorige</button> : <span />}{actieveStapIndex < stappen.length - 1 ? <button type="button" className="primary-button" onClick={() => gaNaarStap(stappen[actieveStapIndex + 1])}>Volgende<ChevronRight size={18} /></button> : <span />}</div>
    </section>
  </main>;
}
